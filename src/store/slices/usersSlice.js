import { createSlice } from "@reduxjs/toolkit";

export default users = createSlice({
    name: 'Users',
    initialState: {
        currentUser: null,
    },
    reducers: {
        setLogin: (state, action) => {

        }
    }
})